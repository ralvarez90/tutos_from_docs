/**
 * HELLO WORLD
 * 
 * Empleamos la función print para imprimir mensajes en
 * consola.
 * 
 * Cualquier programa en dart o flutter tiene un punto de entrada
 * en la función principal main.
 */
void main() {
  // define constant string
  const helloMsg = "Hello World in Dart!";
  print(helloMsg);
}
